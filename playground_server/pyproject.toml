[project]
name = "playground-server"
version = "0.1.0"
description = "Servidor REST modular com FastAPI para curso de integrações de sistemas"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy",
    "pydantic[email]>=2.12.5",
    "loguru>=0.7.3",
    "slowapi>=0.1.9",
]
